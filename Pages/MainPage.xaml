<Page x:Class="PagesWpf.Pages.MainPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:PagesWpf.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Background="Beige"
      FontFamily="Georgia"
      Title="MainPage">


    <Grid Margin="10,10,10,10" Background="AliceBlue">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
            <Image Source="/Images/logo.png" Height="32" Width="100" RenderTransformOrigin="6.083,0.644"/>

            <Button Content="Добавить" Click="Add_Click" Margin="0,0,10,0" Background="Wheat"/>
            <Button Content="Редактировать" Click="Edit_Click" Background="Aqua"/>
            <Button Content="Удалить" Click="Delete_Click" Margin="10,0,0,0" Background="LightCoral"/>
           

        </StackPanel>

        <DataGrid x:Name="MaterialsGrid"
              Grid.Row="1"
              AutoGenerateColumns="False"
              IsReadOnly="False"
              ItemsSource="{Binding}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Id" Binding="{Binding Id}"/>
                <DataGridTextColumn Header="Название" Binding="{Binding Name}"/>
                <DataGridTextColumn Header="Цена" Binding="{Binding Price}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>
